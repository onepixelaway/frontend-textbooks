{
  "name": "frontend-textbooks",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Build, verify, and export designed HTML textbooks.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "book": "node scripts/book-pipeline.mjs",
    "build:book": "node scripts/build-html-book.mjs",
    "check": "node --check scripts/*.mjs && node --check scripts/lib/*.mjs && node --check themes/*.mjs && node --check themes/*/*.mjs && bash -n scripts/*.sh",
    "setup:browsers": "playwright install chromium",
    "test": "node --test --test-concurrency=1 --test-reporter=spec tests/*.test.mjs"
  },
  "dependencies": {
    "markdown-it": "14.3.0",
    "playwright": "1.61.1"
  }
}
